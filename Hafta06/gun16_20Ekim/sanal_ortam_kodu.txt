Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

windows powershell aracılığı ile sanal ortam işlemleri yapmak için yukarıdaki kodu terminalde çalıştır


Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

windows powershell aracılığı ile sanal ortam işlemleri yapmak için yukarıdaki kodu terminalde çalıştır


python -m venv sanal_ortam_adi   # flask_projesi_1


sanal_ortam_adi/Scripts/activate   ile beraber sanal ortamı aktif ediyorduk

deactivate komutu ile sanal ortamı pasifisize ediyoruz


pip list     kurulu kütüphane listesi veriyor 

pip freeze > requirements.txt            ile sistemde kurulu olan kütüphane listesini bir dosyaya yazdırıyoruz

pip install -r requirements.txt          ile sisteme text dosyasında listenenen kütüphaneleri kuruyoruz 